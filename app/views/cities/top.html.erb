<h2>Топ городов по отправлению</h2>
<table class="table table-striped">
    <thead class="table-dark">
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Count</th>
        </tr>
    </thead>
    <tbody>
        <% @cities.each do |city| %>
        <tr>
            <td><%= city["id"] %></td>
                <td><%= city["name"] %></td>
                <td><%= city["count"] %></td>
        <% end %>
        </tr>
    </tbody>
</table>